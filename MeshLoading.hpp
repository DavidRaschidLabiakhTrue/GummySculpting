#ifndef MeshLoading_HPP
#define MeshLoading_HPP
/*
#include "Mesh.hpp"

#include <fstream>
#include <iostream>
#include <streambuf>
#include <stdio.h>

namespace MeshLoadingFunctions::GeneralFileFunctions
{
	void skipFileLines(FILE* file, int linestoSkip);
}

namespace MeshLoadingFunctions::GumLoadingFunctions
{

	void readGumVertex(FILE** file, string& str, Mesh& mesh);

	void readGumIndice(FILE** file, string& str, Mesh& mesh);

	void loadMeshFromGum(string meshFilePath, MeshReference mesh); // loads a mesh with a .gum mesh


   // need to reimplement coloring to accomodate new scale
}

// unlike other header files, MeshLoading only contains functions.
namespace MeshLoadingFunctions
{
	using std::ifstream;
	using std::string;

	using namespace MeshDefinition;
	// these functions are to be defined within their own name spaces for clarity and collapsing
	



	namespace OBJLoadingFunctions
	{
		 void loadMeshFromOBJ(string meshFilePath, MeshReference mesh);  // loads a mesh with a .obj mesh
	}

	// stretch goal, do not focus
	namespace FBXLoadingFunctions
	{
		 void loadMeshFromFBX(string meshFilePath, MeshReference mesh);  // loads a mesh with a .fbx mesh - stretch goal
	}
	
}
*/
#endif // !MeshLoading_HPP

